{
  "name": "monkey-no-fly-zone",
  "version": "1.0.0",
  "description": "A cinematic Three.js survival flight game starring a mechanically winged monkey.",
  "private": false,
  "scripts": {
    "build": "npm run build:game && npm run build:standalone && npm run verify",
    "build:game": "node scripts/build-game.js",
    "build:standalone": "node scripts/build-standalone.js",
    "test": "node --test scripts/*.test.js && npm run verify",
    "verify": "node scripts/verify-game-build.js",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
